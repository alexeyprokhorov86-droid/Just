# TASK_procurement_upd — Автоматизация приёмки УПД и создания ПТУ в 1С

**Старт**: 2026-04-22. **Владелец**: Алексей. **Оценка**: MVP 3-4 недели.

## Пользовательский сценарий (as described)

Кладовщик в TG (позже Element X):
1. Вызывает команду `/receive` → бот просит фото УПД.
2. Загружает 1+ фото УПД, подтверждает «это всё».
3. Бот распознаёт УПД (Claude Vision), показывает кладовщику что понял.
4. Бот находит кандидатов Заказа поставщику по критериям → кладовщик выбирает (если >1).
5. Бот проверяет Соглашение; если >1 действующих — спрашивает в группе Закупки, какое одно реально.
6. Бот просит скан этикетки товара (для серий, если вид номенклатуры требует серий).
7. Бот создаёт ПТУ в 1С → заполняет поля из Заказа/Соглашения/УПД/прочего → прикрепляет сканы → проводит.
8. Бот репортит результат в группу Закупки: успех/неуспех. При НДС — напоминает про счёт-фактуру. Мониторит списание БД-средств (5 мин).

## Критерии кандидата-Заказа поставщику

- проведён (`posted=true`)
- статус `Подтверждён`
- не старше 3 месяцев
- поставщик НЕ в чёрном списке (надо понять где этот список в 1С)
- номенклатура НЕ в группе `Архив` (проверка по иерархии `Catalog_ВидыНоменклатуры`)
- по нему нет приобретений ИЛИ сумма приобретений < сумма заказа

## Поля ПТУ — источники данных

| Поле | Источник |
|---|---|
| Менеджер | Соглашение |
| Поставщик, Контрагент, Валюта, Оплата, Операции, Организация, Склад, ВключаетНДС, Налогообложение, ЗакупкаПодДеятельность, РегистрироватьЦеныПоставщика, МногооборотнаяТара | Заказ поставщику |
| Номенклатура, цена, Ставка НДС % | Заказ (строка) |
| Номенклатура поставщика (как в УПД), Количество | УПД (OCR) |
| № входящего, Дата входящего, Наименование (вид: УПД/…) | УПД (OCR) |
| Номер ПТУ | Автоинкремент в 1С (не задаём) |
| Способ доставки | Константа "Поставщик (на наш склад)" |
| Подразделение | Константа "Управление" |
| Руководитель, ГлавБух | Справочник `Organizations` |
| Счёт контрагента | Справочник `Counterparties` |
| Груз принял | Матчинг TG-user → `v_current_staff` → 1С-user |
| Принял должность | Из `c1_staff_history.position_key` → `c1_positions.name` |
| Автор | OData-пользователь (`odata.user`) |
| Дата поступления | `now()` |
| Комментарий | Автогенерёныйшаблон "Создано автоматически из TG-бота… пользователем… время…" |

## Что уже есть в стеке ✓

- `c1_supplier_orders` + `c1_supplier_order_items` — заказы (мало полей, расширять)
- `c1_purchases` + `c1_purchase_items` — ПТУ (читаем)
- `nomenclature` (7774) — номенклатура
- `c1_employees`, `c1_positions`, `c1_staff_history`, `v_current_staff` — сотрудники/позиции
- `c1_bank_expenses` — чтобы мониторить списание БД-средств
- TG-чат Закупки: **`-1002370366368`** (`tg_chat_1002370366368_bz_zakupki_chat`)
- OData base: `http://185.126.95.33:81/NB_KA/odata/standard.odata` (Basic auth, в `.env`)
- Матчинг TG-user → 1С-сотрудник: `tools/identification.identify_employee_by_text` + `v_current_staff`

## Чего НЕТ (должно появиться)

### Таблицы в БД (фаза 0)
- `c1_supplier_agreements` — Соглашения с поставщиками. Поля минимум: ref_key, supplier_key, organization_key, valid_from, valid_to, is_active, manager_key, currency, operation, payment, nds_mode, pricing_policy.
- `c1_series` — Серии номенклатуры. Ref_key, name, nomenclature_key/warehouse_key/kind_key, дата_изготовления, срок_годности.
- `c1_nomenclature_kinds` — Виды номенклатуры + флаг «использовать серии».
- `c1_counterparties` — Контрагенты (может уже через `partner_key` есть, проверить).
- Расширение `c1_supplier_orders` / `c1_supplier_order_items` — добавить недостающие поля (см. «Поля ПТУ из Заказа»).

### Код
- Write-слой для OData: `tools/onec_write.py` — create/update/post для `Document_ПриобретениеТоваровУслуг`, `Catalog_СерииНоменклатуры`, `Catalog_ПрисоединенныеФайлы`.
- OCR-слой: `tools/vision_upd.py` — Claude Vision API, разбор УПД в структурированный JSON.
- Хендлер приёмки в `bot.py` или новый модуль `receive_flow.py`: ConversationHandler с шагами (загрузка фото → OCR → выбор заказа → подтверждение → этикетки → создание → отчёт).

## План по фазам

### Фаза 0 — Discovery (2-3 дня) ⬅ СТАРТУЕМ ЗДЕСЬ

Цель: снять все технические неизвестные, прежде чем писать пользовательский код.

**Шаги**:

0.1. **OData write smoke-test** — `scripts/odata_probe.py`:
   - `POST /Document_ПриобретениеТоваровУслуг` с минимальным набором полей на тестовом ref_key-комбо (из существующего Заказа).
   - Получить Ref_Key созданного документа.
   - `POST /Document_ПриобретениеТоваровУслуг(<ref>)/Post?PostingModeOperational=false` — провести.
   - Проверить, что в `c1_purchases` после `sync_1c_full --incremental` документ появляется.
   - `POST /Document_ПриобретениеТоваровУслуг(<ref>)/Unpost` + DELETE — откатить.
   - **Deliverable**: рабочая функция `odata_post_doc(doc_type, payload) -> ref_key` + unit-тест.

0.2. **Discovery схемы ПТУ**:
   - `GET /$metadata` — вытянуть XML-схему, распарсить поля `Document_ПриобретениеТоваровУслуг` + `Document_ПриобретениеТоваровУслуг_Товары`.
   - Сохранить в `docs/odata_schema_purchase.md` — для справки.

0.3. **Discovery Соглашений и Серий**:
   - Какие объекты в 1С держат Соглашения: `Catalog_СоглашенияСПоставщиками` vs `Document_…`?
   - Скачать пример одного действующего Соглашения → посмотреть структуру.
   - Серии: `Catalog_СерииНоменклатуры` — какие поля, как привязываются к номенклатуре (характеристика?).
   - **Deliverable**: sample JSON + заметки в `docs/odata_agreements_series.md`.

0.4. **Досинхрон таблиц** — расширить `sync_1c_full.py`:
   - `c1_supplier_agreements` (новая)
   - `c1_series` (новая)
   - `c1_nomenclature_kinds` (новая) + поле `use_series` из карточки вида
   - `c1_counterparties` (если отдельно от partners)
   - расширить `c1_supplier_orders`/`items` недостающими полями из таблицы «Поля ПТУ из Заказа».
   - **Критерий приёмки**: после запуска `sync_1c_full.py --full` все новые таблицы заполнены, старые имеют новые поля, старые значения не потеряны.

0.5. **Claude Vision smoke-test** — `scripts/vision_probe.py`:
   - 2-3 реальных фото УПД (Алексей даёт через `.tmp_input/`).
   - Прогон через `anthropic` SDK с моделью `claude-opus-4-7` + vision + structured output.
   - Получение JSON всех полей из ТЗ (поставщик/покупатель/номенклатура/количества/цены/НДС/печати).
   - Оценка качества: какой процент полей распознан верно, где типичные ошибки.
   - **Deliverable**: `tools/vision_upd.py:extract_upd(images: list[bytes]) -> UpdExtractResult` (pydantic model).

0.6. **Группа Закупки в tools/notifications** — добавить pre-defined recipient group `"purchasing"` → chat_id `-1002370366368`. Проверить `prepare_notification` с её участием.

**Критерий выхода из Фазы 0**: все 6 пунктов зелёные, есть `odata_probe.py` + `vision_probe.py` которые работают, все новые таблицы в БД заполнены.

### Фаза 1 — TG-команда + OCR пайплайн (~3-4 дня)

`/receive` → ConversationHandler → загрузка N фото + кнопка "готово" → `tools/vision_upd.extract_upd()` → показать кладовщику что распознал (отформатированный отчёт) → "подтвердить или переделать". **Без записи в 1С**, финал этапа: JSON-структура УПД в памяти.

### Фаза 2 — Матчинг с Заказом поставщику (~2-3 дня)

- `tools/match_supplier_order.py:find_candidates(upd: UpdExtractResult) -> list[OrderCandidate]` — по критериям из ТЗ.
- UI выбора в TG (InlineKeyboardMarkup с номерами заказов).
- Проверка чёрного списка поставщиков (нужно понять где этот список в 1С).
- **Deliverable**: юнит-тесты на 5-10 разных сценариев матчинга (единственный кандидат, несколько, никого).

### Фаза 3 — Создание ПТУ в 1С (~3-4 дня)

- `tools/procurement_builder.py:build_ptu_payload(order, upd, agreement, user) -> dict` — собирает все поля.
- `odata_post_doc("Document_ПриобретениеТоваровУслуг", payload)` + Post.
- Откат при ошибке (Unpost + DELETE) — важно, чтобы не было зависших документов.
- **БЕЗ вложений и серий** — эти фазы идут следом.

### Фаза 4 — Серии + Этикетки (~2-3 дня)

- Если `nomenclature.kind_key.use_series == True` → запросить фото этикетки.
- `tools/vision_label.extract_label(image) -> LabelData` (производитель, дата изгот, срок годности).
- `odata_post_doc("Catalog_СерииНоменклатуры", ...)` — создать серию.
- Прошить Ref_Key серии в строку ПТУ (обновить созданный документ через PATCH).

### Фаза 5 — Вложения + Отчёты + Мониторинг БД (~2-3 дня)

- Прикрепление сканов УПД и этикеток как `Catalog_ПрисоединенныеФайлы` → связь с ПТУ.
- Отчёт в группу `-1002370366368` при успехе и при ошибке.
- Напоминание про счёт-фактуру если в ПТУ есть НДС.
- Watcher на `c1_bank_expenses`: в течение 5 мин после проведения ПТУ должна появиться запись на эту сумму к этому контрагенту → если нет, алерт в Закупки.

### Фаза 6 — Соглашения: валидация + вопрос в Закупки (~1-2 дня)

- При сборе payload для ПТУ: найти действующие Соглашения для (поставщик × организация × на_дату).
- Если 1 — используем, если 0 — алерт (нельзя создать), если >1 — вопрос в группу Закупки с inline-кнопками "1/2/3", после ответа — пометить выбранное как единственно-действующее, остальные — флаг в нашей БД `is_manually_invalidated=true` (не трогаем 1С-карточку, это делает человек).

## Риски

1. **OData write недоступен / ограничен** — например, нельзя создавать ПТУ через OData (только через Web-форму). Митигация: Фаза 0.1 делает smoke-test на минимальном документе; если не работает — эскалация до старта Фазы 1.
2. **Качество OCR на мятых сканах / рукописных пометках** — проверяем на 2-3 образцах в Фазе 0.5. Если <80% полей верно, нужен fallback (ручная корректировка перед созданием ПТУ).
3. **Временная рассинхронизация `c1_*`** — бот создаёт ПТУ в 1С, но в нашей БД он появится только после `sync_1c_full --incremental`. Решение: после `Post` сразу дёргаем `sync_1c_full --incremental` для `c1_purchases` (или обновляем точечно).
4. **Соглашение "действующее" — нечёткое понятие** — в 1С может быть не один флаг, а комбинация (срок × дата документа × приоритет). Уточнить с Алексеем в Фазе 0.3.
5. **Тест-база vs прод** — Алексей сказал «будем тестить сначала». Детали: есть ли отдельная база, или это прод, но с соглашением аккуратности? Уточнить.

## Прогресс

### Фаза 0 — в работе (2026-04-22)

- [ ] 0.1 OData write smoke-test
- [ ] 0.2 Схема ПТУ
- [ ] 0.3 Соглашения и Серии discovery
- [ ] 0.4 Досинхрон таблиц
- [ ] 0.5 Claude Vision smoke-test (жду фото УПД от Алексея в `.tmp_input/`)
- [ ] 0.6 Группа Закупки в tools/notifications
